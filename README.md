This structure enforces sepration of concerns.

Lay Roles:

Controllers → HTTP requests
Services → business logic
Repositories → data access
Models → domain objects
Dependencies → dependency injection